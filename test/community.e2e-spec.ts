describe('Community endpoints (e2e)', () => {
	it.todo('POST /communities creates community and returns payload')
	it.todo('POST /communities/:communityId/join joins public community')
	it.todo(
		'POST /communities/:communityId/members/:userId/ban bans target by hierarchy'
	)
})
